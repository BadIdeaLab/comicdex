/// One comic carrying one tag, among the comics the user kept (P83).
class TagAssignment {
  const TagAssignment({required this.comicId, required this.tagId});

  final String comicId;
  final int tagId;
}
